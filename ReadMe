Bootstrap-Repository
Mobile responsive Web App built using Bootstrap and CSS, HTML & Javascript.

Uses Bootstraps in bullt classes such as jumbotron and col-md-3 (or 6/9/12) to arrange the page in a grid system
The css does not require handling of mobile responsive device widths using the bootstrap libraries


